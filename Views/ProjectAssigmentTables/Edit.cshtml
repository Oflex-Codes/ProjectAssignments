@model ProjectAssignments.Models.ProjectAssigmentTable

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>ProjectAssigmentTable</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.EmpName)

        @Html.HiddenFor(model => model.ProjectName)

        @Html.HiddenFor(model => model.ProjectManager)

        @Html.HiddenFor(model => model.ProjectStartDate)

        @Html.HiddenFor(model => model.ProjectEndDate)

        @Html.HiddenFor(model => model.Department)

        @Html.HiddenFor(model => model.Admin)

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
